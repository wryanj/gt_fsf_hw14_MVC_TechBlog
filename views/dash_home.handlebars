
<!--Difference between dash and home is based on logged in status. dash includes new button, and delete / edit buton and script-->
<!--Edit / delete conditional is here in this partial, create new button conditional is in the main handlebars-->

<!--Loop Through Each Object in Blogs Array and create a card-->
{{#each blogs as |blog|}}

<div class="row mt-4">

    <div class="card text-center px-0 py-0 shadow p-3 mb-3 bg-body rounded">
        <div class="card-header bg-light">
            Date
        </div>
        <div class="card-body">
            <h5 class="card-title">{{blog.title}}</h5>
            <p class="card-text">{{blog.content}}</p>
        </div>
        <div class="card-footer bg-light">
            {{blog.user.user_name}}
        </div>
    </div>

    <!--Conditionally show edit and delete buttons on blog cards based on login status-->
    {{#if logged_in}}
    <div class="text-center">
        <button type="button" class="btn btn-warning mb-3 shadow-sm">
            <i class="fas fa-pencil-alt"></i>
        </button>
        <button type="button" class="btn btn-danger mb-3 shadow-sm">
            <i class="far fa-trash-alt"></i>
        </button>
    </div>
    {{/if}}

</div>

{{/each}}



